---
import PhotoTile from './PhotoTile.astro';
import type { PhotoItem } from '../lib/photos';

interface Props {
  items: PhotoItem[];
  groupId?: string;
}

const { items, groupId = 'gallery' } = Astro.props;
---

<div class="gallery">
  {items.map((item) => <PhotoTile image={item.image} alt={item.alt} size={item.size} groupId={groupId} />)}
</div>
