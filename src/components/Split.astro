---
interface Props {
  reverse?: boolean;
  /** Padding + a bottom hairline, for a list of Split rows stacked inside one Section. */
  divider?: boolean;
}

const { reverse = false, divider = false } = Astro.props;
---

<div class:list={['split', { reverse, divider }]}>
  <slot />
</div>
