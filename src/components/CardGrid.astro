---
interface Props {
  /** Desktop column count override; default is a responsive 1/2/3. */
  cols?: 2 | 4 | 5;
}

const { cols } = Astro.props;
---

<div class:list={['card-grid', cols && `cols-${cols}`]}>
  <slot />
</div>
